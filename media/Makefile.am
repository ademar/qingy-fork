## Makefile.am for qingy/media

SUBDIRS = default sessions pam screensavers

dist_lisp_LISP = qingy-mode.el

settingsdatadir = $(prefix)/etc/qingy
dist_settingsdata_DATA = \
		settings             \
		welcomes
